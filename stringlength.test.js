const stringLength = require('./stringLength');

describe('Return Length of a string', () => {
  it('should return the length of a string', () => {
    expect(stringLength('Abu Talha')).toBe(9);
  });
});
 // arrange


  // assert

